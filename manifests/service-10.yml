apiVersion: v1
kind: Service
metadata:
    name: "diaggarwtest-cf3f"
    labels:
        app: "diaggarwtest-cf3f"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "diaggarwtest-cf3f"