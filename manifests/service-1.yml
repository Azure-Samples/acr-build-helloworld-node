apiVersion: v1
kind: Service
metadata:
    name: "diaggarwtest-f24e"
    labels:
        app: "diaggarwtest-f24e"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "diaggarwtest-f24e"