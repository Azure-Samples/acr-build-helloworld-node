apiVersion: v1
kind: Service
metadata:
    name: anuragcacrbuildhelloworldnode
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: anuragcacrbuildhelloworldnode