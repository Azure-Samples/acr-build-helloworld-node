apiVersion: v1
kind: Service
metadata:
    name: "diaggarwtest-720f"
    labels:
        app: "diaggarwtest-720f"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "diaggarwtest-720f"