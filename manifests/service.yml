apiVersion: v1
kind: Service
metadata:
    name: "anchauhakscluster"
    labels:
        app: "anchauhakscluster"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "anchauhakscluster"