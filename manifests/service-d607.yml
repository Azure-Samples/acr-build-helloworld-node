apiVersion: v1
kind: Service
metadata:
    name: "diaggarwtest-6a0c"
    labels:
        app: "diaggarwtest-6a0c"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "diaggarwtest-6a0c"
