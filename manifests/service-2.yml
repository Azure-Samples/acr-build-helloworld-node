apiVersion: v1
kind: Service
metadata:
    name: "diaggarwtest-bac1"
    labels:
        app: "diaggarwtest-bac1"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "diaggarwtest-bac1"