apiVersion: v1
kind: Service
metadata:
    name: "aaaaatiwarishubaks-e824"
    labels:
        app: "aaaaatiwarishubaks-e824"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "aaaaatiwarishubaks-e824"