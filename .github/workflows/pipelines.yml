on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    
    - uses: azure/k8s-actions/docker-login@master
      with:
        login-server: anuragcontainerwebappacr.azurecr.io
        username: ${{ secrets.REGISTRY_USERNAME-5010 }}
        password: ${{ secrets.REGISTRY_PASSWORD }}
    
    - run: |
        docker build . -t anuragcontainerwebappacr.azurecr.io/anchauhakscluster7844:${{ github.sha }}
        docker push anuragcontainerwebappacr.azurecr.io/anchauhakscluster7844:${{ github.sha }}
    
    - uses: azure/k8s-actions/k8s-set-context@master
      with:
         kubeconfig: ${{ secrets.KUBE_CONFIG }}
      id: login
    
    - name:  create namespace
      run: |
        kubectl create namespace anchauhaksclusterfb0f

    - uses: azure/k8s-actions/k8s-create-secret@master
      with:
        namespace: anchauhaksclusterfb0f
        container-registry-url: anuragcontainerwebappacr.azurecr.io
        container-registry-username: ${{ secrets.REGISTRY_USERNAME-5010 }}
        container-registry-password: ${{ secrets.REGISTRY_PASSWORD }}
        secret-name: dockerAuthSecret
       
    - uses: azure/k8s-actions/k8s-deploy@master
      with:
        manifests: |
          manifests/deployment.yml
          manifests/service.yml
        images: |
          anuragcontainerwebappacr.azurecr.io/anchauhakscluster7844:${{ github.sha }}
        imagepullsecrets: |
          dockerAuthSecret