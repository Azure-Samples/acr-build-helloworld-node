on: [push]
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    
    - uses: actions/checkout@master
    
    - uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Create ACR
      id: create-acr
      run: |
        az acr create -n anuNewacr -g anuNewacrb401-rg --location "East US" --sku Standard --admin-enabled
        echo "::set-output name=acr_username::`az acr credential show -n anuNewacr --query username | xargs`"
        echo "::set-output name=acr_password::`az acr credential show -n anuNewacr --query passwords[0].value | xargs`"
        echo "::add-mask::`az acr credential show -n anuNewacr --query passwords[0].value | xargs`"

    - uses: azure/docker-login@v1
      with:
        login-server: anunewacr.azurecr.io
        username: ${{ steps.create-acr.outputs.acr_username }}
        password: ${{ steps.create-acr.outputs.acr_password }}

    - name: Build and push image to ACR
      id: build-image
      run: |
        docker build "$GITHUB_WORKSPACE/" -f  "Do - cker/Dockerfile" -t anunewacr.azurecr.io/aaaaatiwarishubaks:${{ github.sha }}
        docker push anunewacr.azurecr.io/aaaaatiwarishubaks:${{ github.sha }}
    
    - uses: azure/k8s-set-context@v1
      id: login
      with:
         kubeconfig: ${{ secrets.aks_aaaaatiwarishubaks_kubeConfig }}
    
    - name: Create namespace
      run: |
        namespacePresent=`kubectl get namespace | grep aaaaatiwarishub-aks8c2f | wc -l`
        if [ $namespacePresent -eq 0 ]
        then
            echo `kubectl create namespace aaaaatiwarishub-aks8c2f`
        fi

    - uses: azure/k8s-create-secret@v1
      with:
        namespace: aaaaatiwarishub-aks8c2f
        container-registry-url: anunewacr.azurecr.io
        container-registry-username: ${{ steps.create-acr.outputs.acr_username }}
        container-registry-password: ${{ steps.create-acr.outputs.acr_password }}
        secret-name: aaaaatiwarisdockerauth
       
    - uses: azure/k8s-deploy@v1
      with:
        namespace: aaaaatiwarishub-aks8c2f
        manifests: |
          manifests/deployment.yml
          manifests/service.yml
        images: |
          anunewacr.azurecr.io/aaaaatiwarishubaks:${{ github.sha }}
        imagepullsecrets: |
          aaaaatiwarisdockerauth