on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    
    - uses: azure/containers-actions/docker-login@master
      with:
        login-server: anuragcontainerwebappacr.azurecr.io
        username: ${{ secrets.REGISTRY_USERNAME-184c }}
        password: ${{ secrets.REGISTRY_PASSWORD }}
    
    - run: |
        docker build . -t anuragcontainerwebappacr.azurecr.io/anchauhakscluster41d8:${{ github.sha }}
        docker push anuragcontainerwebappacr.azurecr.io/anchauhakscluster41d8:${{ github.sha }}
    
    - uses: azure/k8s-actions/k8s-set-context@master
      with:
         kubeconfig: ${{ secrets.KUBE_CONFIG }}
      id: login
    
    - name:  create namespace
      run: |
        kubectl create namespace anchauhaksclusterf64b

    - uses: azure/k8s-actions/k8s-create-secret@master
      with:
        namespace: anchauhaksclusterf64b
        container-registry-url: anuragcontainerwebappacr.azurecr.io
        container-registry-username: ${{ secrets.REGISTRY_USERNAME-184c }}
        container-registry-password: ${{ secrets.REGISTRY_PASSWORD }}
        secret-name: dockerAuthSecret
       
    - uses: azure/k8s-actions/k8s-deploy@master
      with:
        manifests: |
          manifests/deployment-4.yml
          manifests/service-4.yml
        images: |
          anuragcontainerwebappacr.azurecr.io/anchauhakscluster41d8:${{ github.sha }}
        imagepullsecrets: |
          dockerAuthSecret