on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    
    - uses: azure/containers-actions/docker-login@master
      with:
        login-server: anuragcontainerwebappacr.azurecr.io
        username: ${{ secrets.REGISTRY_USERNAME-b47d }}
        password: ${{ secrets.REGISTRY_PASSWORD }}
    
    - run: |
        docker build . -t anuragcontainerwebappacr.azurecr.io/anchauhaksclusterd6a2:${{ github.sha }}
        docker push anuragcontainerwebappacr.azurecr.io/anchauhaksclusterd6a2:${{ github.sha }}
    
    - uses: azure/k8s-actions/k8s-set-context@master
      with:
         kubeconfig: ${{ secrets.KUBE_CONFIG }}
      id: login
    
    - name:  create namespace
      run: |
        kubectl create namespace anchauhakscluster543a

    - uses: azure/k8s-actions/k8s-create-secret@master
      with:
        namespace: anchauhakscluster543a
        container-registry-url: anuragcontainerwebappacr.azurecr.io
        container-registry-username: ${{ secrets.REGISTRY_USERNAME-b47d }}
        container-registry-password: ${{ secrets.REGISTRY_PASSWORD }}
        secret-name: dockerAuthSecret
       
    - uses: azure/k8s-actions/k8s-deploy@master
      with:
        manifests: |
          manifests/deployment-5.yml
          manifests/service-5.yml
        images: |
          anuragcontainerwebappacr.azurecr.io/anchauhaksclusterd6a2:${{ github.sha }}
        imagepullsecrets: |
          dockerAuthSecret